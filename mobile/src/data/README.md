# Data layer

Concrete implementations: the local SQLite repository (cached Pokémon data), the backend API
client (for Pro features only), and the native module bridge for the Android overlay + ML Kit OCR.
