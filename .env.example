# Copy this file to .env in the repo root (gitignored) before running `docker compose up`.
# These values must match backend/.env's DATABASE_URL (user, password, db name, port).

POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=pokemon_trainer_companion
POSTGRES_PORT=5432
